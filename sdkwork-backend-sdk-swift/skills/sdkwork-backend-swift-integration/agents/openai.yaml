interface:
  display_name: "SDKWORK Backend swift Integration"
  short_description: "Integrate sdkwork-backend-sdk-swift with staged progressive loading."
  default_prompt: "Use $sdkwork-backend-swift-integration to integrate sdkwork-backend-sdk-swift into my application with staged progressive loading."

policy:
  allow_implicit_invocation: true

