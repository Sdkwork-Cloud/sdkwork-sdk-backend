interface:
  display_name: "SDKWORK Backend kotlin Integration"
  short_description: "Integrate sdkwork-backend-sdk-kotlin with staged progressive loading."
  default_prompt: "Use $sdkwork-backend-kotlin-integration to integrate sdkwork-backend-sdk-kotlin into my application with staged progressive loading."

policy:
  allow_implicit_invocation: true

